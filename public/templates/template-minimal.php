<?php
/**
 * Minimal + Glass + Classic share the same panel structure as Modern.
 * Include modern template as the base, they just differ in CSS.
 */
defined( 'ABSPATH' ) || exit;
include __DIR__ . '/template-modern.php';
